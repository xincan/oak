<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.tarotframework.server.oak.mapper.IMhDataScourMapper">

    <select id="getMhHourDetail" resultType="cn.com.tarotframework.server.oak.po.MhHourDetail">
        SELECT * FROM mh_hour_detail mhd where mhd.hour_id = (
            SELECT muh.id hour_id FROM mh_user_hour muh WHERE muh.user_id = #{userId} AND muh.fill_date = #{date}
        )

    </select>

</mapper>
